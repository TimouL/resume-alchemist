# Resume Alchemist - 后端服务器配置
# 复制此文件为 .env.server 并填写实际值

# ============================================
# 数据库配置
# ============================================

# 数据库类型：sqlite（默认）或 supabase
DATABASE_TYPE=sqlite

# SQLite 数据库文件路径（仅 DATABASE_TYPE=sqlite 时使用）
SQLITE_DB_PATH=./data/resume-alchemist.db

# Supabase 配置（仅 DATABASE_TYPE=supabase 时使用）
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# AI 服务配置（支持 OpenAI 兼容接口）
# ============================================

# API 密钥（必填）
# 支持：硅基流动、OpenAI、Azure OpenAI、Ollama 等
OPENAI_API_KEY=your-api-key-here

# API 端点（可选，默认硅基流动）
# 硅基流动: https://api.siliconflow.cn/v1/chat/completions
# OpenAI:   https://api.openai.com/v1/chat/completions
# Ollama 本地: http://host.docker.internal:11434/v1/chat/completions
# 局域网服务: 确保 Docker 容器可以访问该网络
OPENAI_API_BASE=https://api.siliconflow.cn/v1/chat/completions

# AI 模型名称（可选，默认 Qwen/Qwen3-8B）
# 硅基流动: Qwen/Qwen3-8B, deepseek-ai/DeepSeek-V3, etc.
# OpenAI:   gpt-4o, gpt-4o-mini, gpt-3.5-turbo
# Ollama:   llama3.2, qwen2.5, deepseek-r1
OPENAI_MODEL=Qwen/Qwen3-8B

# ============================================
# 服务器配置
# ============================================

# 服务器端口（默认 8000）
PORT=8000

# 是否启用速率限制（默认关闭）
RATE_LIMIT_ENABLED=false
